const generateCopyLink = (copyLinkButtonElement) => {
    console.log('copyLinkButtonElement', copyLinkButtonElement);
    // const copyLink = copyLinkButtonElement;
    // copyLink.addEventListener('click', function (e) {
    //     e.preventDefault(); // Prevent the default action of the link
    //     const tempInput = document.createElement('input');
    //     tempInput.value = copyLink.href; // Set its value to the link's href
    //     document.body.appendChild(tempInput); // Append it to the body
    //     // Select the text inside the input element
    //     tempInput.select();
    //     tempInput.setSelectionRange(0, 99999); // For mobile devices
    //     // Copy the selected text to the clipboard
    //     document.execCommand('copy');
    //     // Remove the temporary input element
    //     document.body.removeChild(tempInput);
    //     // Optionally, show an alert or message to indicate the link was copied
    //     alert('Link copied to clipboard!');
    // });
};